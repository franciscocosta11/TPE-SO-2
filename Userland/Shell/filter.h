#ifndef FILTER_H
#define FILTER_H

/**
 * @brief Entrada del comando filter: elimina vocales de la entrada y escribe el resto.
 */
#include <stdint.h>

void filter_entry(uint64_t argc, char **argv);

#endif
